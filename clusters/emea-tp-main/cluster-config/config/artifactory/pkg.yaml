apiVersion: data.packaging.carvel.dev/v1alpha1
kind: Package
metadata:
  name: artifactory.timosalm.de.1.0.0
  namespace: tp-install
spec:
  refName: artifactory.timosalm.de
  version: "1.0.0"
  template:
    spec:
      fetch:
      - helmChart:
          name: artifactory-oss
          version: 107.90.8
          repository:
            url: https://charts.jfrog.io
      template:
      - helmTemplate:
          namespace: artifactory
      - ytt:
          inline:
            paths:
              overlay.yml: |
                #@ load("@ytt:overlay", "overlay")
                #@overlay/match by=overlay.subset({"kind":"PodDisruptionBudget"}),expects="0+"
                #@overlay/remove
                ---
      deploy:
      - kapp: {}